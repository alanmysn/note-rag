# AGENTS.md — note-rag 项目协作规则

> 本文件给 AI 助手看：进入本项目后按这里的规矩干活（助手打开项目会自动读取本文件）。

## 项目一句话

note-rag：本地 Obsidian 笔记库（路径在本地配置中指定，不入库）的 RAG 问答机器人，答案必须附出处。需求全文见 `01-需求记录.md`。

## 协作流程（不可跳过）

1. 五阶段推进：需求探讨 → 功能规划 → 方案设计 → 分步建造 → 迭代打磨
2. 用户是产品决策者：每个取舍摆出大白话选项 + 助手的推荐，由用户拍板并记录日期
3. 每一步产出必须用户亲手验收后才算完成；未验收不得进入下一步
4. 任何具体产出（文档/代码/改文件）动笔前先问「是否开始写」并获确认

## 文档约定

- 根目录编号流程文档：`01-需求记录.md` → `02-功能规划.md` → `03-方案设计.md` → `04-开发计划.md`
- `README.md` 是门面：进度清单只勾用户已验收的步骤；功能/技术栈随实现更新
- **README 按验收点同步**：每验收一步，当轮就把 README 更新到「该步已完成」状态，不追实时、不拖到下次
- 文档先行、与实现同步：讨论改变了结论，当轮就改文档，不允许文档说 A、代码做 B
- 决策记录带日期（「已确认 YYYY-MM-DD」）；暂定规则标「暂定」，不确定的解读标 ⚠️

## Git 约定

- 本地 init/commit 由 AI 命令行执行；push 由用户在 GitHub Desktop 操作（Publish/Push）
- 提交信息：约定式前缀（docs/feat/fix/chore）+ 一句话中文
- 文档与代码分开提交；每验收一步提交一次
- 仓库目标公开（面试展示）：保持干净历史，密钥/本地配置永不入库

## 红线（不可违反）

1. 对本地 Obsidian 笔记库（路径见本地配置）**只读**：绝不修改、移动、删除任何笔记文件
2. 不擅自添加未讨论过的 UI 元素、功能、提示文字——哪怕觉得 helpful，也先在对话里提出
3. 密钥、API key、cookies、本地绝对路径等敏感/个人化信息只放 gitignore 的本地文件，不进 git
4. 向用户提问用对话文字列选项，不用表单式勾选（勾选会丢失附言）

## 架构与关键决策（随方案设计更新）

- 细节见 `03-方案设计.md`（待建）；此处只放结论级摘要与依据，不抄正文
- 已定方向（2026-08-28）：云端大模型 API（隐私红线当前宽松）；电脑跑本地服务 + 手机浏览器访问；检索参数走配置文件
