# note-rag · 开发计划（v1）

> 记录日期：2026-08-28
> 状态：待用户验收
> 位置：项目根目录（本仓库）
> 上一站：03-方案设计.md（已验收）

## 0. 总则

- 分 9 步建造，每步**只有一个看得见的成果**，用户亲手验收后才进下一步
- 每步单独提交（`feat:` 前缀），文档与代码分开提交
- 验收通过后勾选 README 进度清单
- 代码统一放 `app/` 目录；原型保留在 `prototype/`（不参与运行）
- 红线全程生效：笔记库只读、API key 只进 `.env`（gitignore）、不擅自加未讨论的 UI/功能

## 1. 第 1 步：项目骨架与配置

**成果**：`python app/main.py config` 能打印出配置内容

**内容**：
- `app/` 目录结构；`requirements.txt`
- `config.example.yaml`（入库，占位无真实路径）；`config.yaml` 与 `.env`（gitignore，真实值本地生成）
- 配置加载模块：读取 yaml + .env，缺 key 时给出友好报错
- `.gitignore` 完善（.env、config.yaml、索引数据目录、__pycache__ 等）

**验收**：跑配置命令，看到笔记库路径、切块大小、top-k 等参数正确打印

## 2. 第 2 步：读笔记库 + 切块

**成果**：命令行输出「共 N 篇笔记，切成 M 块」

**内容**：
- 遍历笔记库（排除 exclude_dirs，如 `.obsidian`），只读
- 按标题/段落优先切块，超长段落按字数硬切，带重叠（chunk_size=500、overlap=50，配置可调）
- 统计命令：`python app/main.py stats`

**验收**：跑 stats，篇数与块数符合预期（可用你的真实笔记库验证）

## 3. 第 3 步：向量化 + 建索引（Chroma）

**成果**：`python app/main.py index` 完成建索引，显示入库块数

**内容**：
- 加载本地 BGE-M3（首次自动下载，约 1-2 GB）
- 每块向量化，连同原文、出处（文件路径）存入 Chroma
- 幂等：重复建索引不产生重复块（先清后建）
- 索引数据目录 gitignore

**验收**：跑 index，块数与第 2 步统计一致；重跑一遍数量不变

## 4. 第 4 步：检索

**成果**：`python app/main.py search "问题"` 输出最相关的几块原文

**内容**：
- 问题向量化（同一 BGE-M3）
- Chroma 相似度检索：top-k + 相似度门槛（低于门槛判「没找到」）
- 命令行输出：命中块原文 + 相似度 + 出处文件

**验收**：用真实问题（如「打印机」）能捞出相关片段；问不存在的主题能明确显示「未命中」

## 5. 第 5 步：DeepSeek 生成 + 出处编号

**成果**：`python app/main.py ask "问题"` 输出答案 + 出处编号列表

**内容**：
- 提示词拼接：系统规则（只能依据资料、没有就说没有）+ 编号资料 + 问题
- 调 DeepSeek（key 从 .env 读）
- 答案中引用编号 `[1][2]`，程序端映射回出处（文件 + 原文片段）
- 无命中时走 A2：提示词明确要求直说「没找到」

**验收**：命令行问答，答案有依据、出处真实对应；问库里没有的东西，AI 直说没找到

## 6. 第 6 步：FastAPI 服务 + 网页界面（原型 B）

**成果**：电脑浏览器打开页面，能提问、答案带出处卡片

**内容**：
- FastAPI 起服务（绑定 127.0.0.1 与局域网 IP）
- 前端按原型 B 形态实现：搜索框 + 回答卡 + 出处卡列表 + 未命中提示卡
- 后端接口：提问 → 检索 → 生成 → 返回答案 + 出处卡片
- 流式输出（A5，打字机效果，顺带实现）

**验收**：电脑浏览器提问，回答与出处显示正常；无命中场景显示提示卡

## 7. 第 7 步：手机访问

**成果**：手机浏览器（同一 WiFi）能打开并提问

**内容**：
- 确认服务绑定 0.0.0.0，防火墙放行端口
- 手机访问 `http://电脑IP:端口`，界面适配手机屏幕（响应式）

**验收**：手机浏览器提问，得到与电脑一致的答案与出处

## 8. 第 8 步：增量更新 + 手动重建

**成果**：新写/修改笔记后，重启服务即被检索到；页面有「重建索引」入口

**内容**：
- 启动时比对文件修改时间/内容指纹，只对变更文件重新切块+向量化
- 删除的文件从索引清除
- 手动重建：清空索引全量重建（B3）

**验收**：新建一篇测试笔记 → 重启 → 能被问到；删除一篇 → 重启 → 不再被问到；重建按钮可用

## 9. 第 9 步：打磨与收尾

**成果**：README 完整可发布，整体流程走查通过

**内容**：
- 错误处理（API 失败、模型未下载、路径不存在等给友好提示）
- README 补全：功能、技术栈、运行方法、文档索引、进度
- 全流程走查 + 清理临时代码

**验收**：按 README 从零跑通一次；日常使用无异常提示

## 10. v2 池子（本阶段不做）

A4 多轮追问、B4 实时监听、C2 问答历史、C3 索引状态页、D3 出门可用、embedding 切云端
