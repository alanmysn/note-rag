# note-rag · 方案设计（v1）

> 记录日期：2026-08-28
> 状态：原方案已验收；2026-09-08 切块修订由用户授权助手验收通过；真实索引已重建并通过现有题库的基础验证
> 位置：项目根目录（本仓库）
> 上一站：02-功能规划.md（已验收）

## 1. 技术选型（全部已拍板，带日期）

| 组件 | 选择 | 决策日期 | 备注 |
|---|---|---|---|
| 生成模型 | DeepSeek API | 2026-08-28（暂定） | key 只存本地 `.env`（gitignore），模型名等参数配置化，可随时换 |
| 向量化模型 | **本地 BGE-M3** | 2026-08-28 | 免费、笔记不出电脑、CPU 可跑；配置留「embedding 来源」开关，将来可切云端 |
| 向量库 | **Chroma** | 2026-08-28 | 嵌入式（无服务器）、开源免费、存「向量+原文+出处」一体 |
| 服务框架 | Python 3.11 + FastAPI | 2026-08-28 | 电脑本地跑服务，手机同 WiFi 浏览器访问 |
| 前端 | 静态 HTML/CSS/JS（无框架） | 2026-08-28 | 延续个人项目风格，轻量免构建 |
| 界面形态 | **原型 B：检索卡式** | 2026-08-28（用户拍板） | 顶部搜索框 + 回答卡 + 出处卡列表 |
| 出门在外可用（D3） | 暂缓 | 2026-08-28（无异议通过） | 进 v2 池子，见 §6 |

## 2. 架构与数据流

```
                    ┌─────────────── 电脑（本地） ───────────────┐
 Obsidian 笔记库 ──▶ 切块 ──▶ BGE-M3 向量化 ──▶ Chroma 存储
 (只读)               │            (本地模型)        (向量+原文+出处)
                      └──────────────────────────────────────────┘

 手机 / 电脑浏览器 ──▶ FastAPI 服务
       │                    │
       │                    ├─▶ 问题向量化（BGE-M3）──▶ Chroma 检索
       │                    │        （top-k + 相似度门槛）
       │                    ├─▶ 拼提示词（资料编号[1][2]…）──▶ DeepSeek
       │                    └─▶ 返回：答案 + 编号映射的出处卡片
       ◀────────────────────┘
```

要点：

- 笔记库**只读**：程序只读文件内容，绝不写入、改名、移动
- 检索参数全部在配置文件，不前期定死，用测试题库迭代调
- 出处机制（防 AI 编造）：程序给资料块编号 `[1][2]…`，AI 只引用编号，程序端映射回真实出处卡片

### 切块与上下文恢复（已确认并授权助手验收通过 2026-09-08）

- 原笔记只读，正文不经 AI 改写；规则切块，不引入 AI 切块。
- Markdown 标题分别划分范围，不跨标题合并；独立加粗行作为小节标题，保留上级 Markdown 标题。分隔线是边界。
- 同范围相邻短段按目标 500 字符合并，不拆下一段凑数；完整段落 500–800 字符单独保留。正文上限 800，文件名和标题链另计。
- 超长段落先按句末、再按句内标点、最后按字符硬切，不预先重叠。
- 文件开头的 YAML 属性区暂不参与检索，不加入标题链；正文分隔线仍是边界。
- 列表连同紧邻引导段作为一个原始单元；按顶层完整列表项拆，子列表随父项，保留编号。单项超长再按段落和句子拆。紧邻的短说明（不超过合并目标）无需冒号也与列表关联；长说明仍用冒号判断。短引导段可重复，过长的单独切分但共同恢复。
- 表格尽量完整，超长按完整数据行拆，每块保留表头、分隔行及短引导段；命中恢复引导段与整表。单行加表头允许超过 800 字符；建索引前检查向量模型实际输入容量，超出则明确报错，禁止静默截断。
- 同标题内，表格前后顶层有序列表编号连续（如 3 后接 4）、缩进一致，且没有独立正文或其他边界时，合为一个恢复单元。表格仍按行切，任一部分命中均恢复完整步骤与表格；不连续编号、无序列表不自动跨表格关联。
- 每块向量化文本包含文件名、标题链；保存原始单元标识和完整正文快照。长列表分块可重复原文引导句；过长引导段独立切分，仍归同一原始单元。
- 命中后恢复完整原始段落或列表；短段合并块保持合并后的正文。同源单元去重，保留最高命中分数，不跨单元自动扩展。
- 不另设小字符预算；第 5 步接模型时检查全部输入容量并预留输出，超限明确报错，不静默截断。该检查尚未实现。
- 新索引使用切块版本标记；旧索引需用户确认重建后才可使用新检索，不能混用。本轮已获确认重建，旧索引备份保留。
- `chunk_size` 为合并目标，新增 `chunk_max_size` 为正文上限（默认 800）；旧 `chunk_overlap` 停用，本地旧配置允许保留但不再生效。

## 3. 界面定稿（原型 B 形态）

顶部搜索框 → 提问后：

1. **回答卡**：AI 答案正文（带编号引用 [1][2]），如「依据 2 篇笔记」
2. **出处卡列表**：每篇命中的笔记一张卡——文件名 + 相似度 + 原文片段

无命中时显示「全库检索未命中相似度门槛」提示卡，AI 不硬编。

原型文件：`prototype/b-search.html`（正式版按此形态实现，配色/细节可微调，不做结构性改动）

## 4. 配置文件与安全

两个本地文件（都不入库）：

| 文件 | 内容 | 示例 |
|---|---|---|
| `.env`（gitignore） | API key | `DEEPSEEK_API_KEY=sk-xxx` |
| `config.yaml`（gitignore） | 笔记库路径、检索参数 | 见下 |

`config.yaml` 字段（暂定默认值，迭代期可调）：

```yaml
vault_path: <本地 Obsidian 笔记库路径>   # 不入库
data_dir: data                          # 索引与模型存放目录（相对项目根，gitignore，不入库）
embedding_provider: local-bge-m3        # 可选：local-bge-m3 / 云端（v2）
chunk_size: 500                         # 短段合并目标（字符）
chunk_max_size: 800                     # 单块正文上限（字符）
top_k: 5                                # 每次检索捞几块
similarity_threshold: 0.5               # 相似度门槛，低于此判「没找到」
exclude_dirs: [.obsidian]               # 排除的文件夹
```

说明：`.env` 与 `config.yaml` 在仓库中**不存在**，仓库提供 `config.example.yaml`（占位、无路径无 key）供参考。

### 存储位置约定（2026-08-28 确认：全部收进项目内，C 盘零占用）

| 数据 | 位置 | 说明 |
|---|---|---|
| Python 虚拟环境 | `项目根/.venv/` | gitignore |
| Chroma 索引 | `项目根/data/chroma/` | gitignore |
| BGE-M3 模型（1-2 GB） | `项目根/data/models/` | gitignore；通过环境变量把模型缓存指到此处，避免默认装进 C 盘 |
| `.env` / `config.yaml` | `项目根/` | gitignore |
| 网页前端静态文件 | `项目根/app/static/` | 入库 |

整个 `项目根/` 文件夹可整体拷贝迁移（配置、模型、索引一起走）。

## 5. 依赖清单（开发计划阶段安装）

- `fastapi` + `uvicorn`（web 服务）
- `chromadb`（向量库）
- `sentence-transformers` + `BGE-M3` 模型（本地向量化，首次下载约 1–2 GB）
- `openai` SDK 或 `requests`（调 DeepSeek，OpenAI 兼容接口）
- `python-dotenv`（读 .env）

## 6. v2 池子（暂缓/待迭代）

A4 多轮追问、B4 实时监听、C2 问答历史、C3 索引状态页、D3 出门可用（内网穿透/云部署）、embedding 切云端 API

## 7. 移交下一阶段（04-开发计划）的待决问题

- [x] 切块策略细则：2026-09-08 确认，见切块与上下文恢复；切块及恢复逻辑验收通过
- [ ] BGE-M3 首次下载与 CPU 速度实测
- [ ] 启动时增量更新的文件指纹方案（修改时间 or 内容哈希）
- [ ] 开发计划拆步与验收标准
